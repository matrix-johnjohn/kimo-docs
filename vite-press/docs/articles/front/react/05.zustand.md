---
title: zustand
date: 2025-03-07 01:16:28
permalink: /articles/front/react/zustand
categories:
  - 前端
tags:
  - react
titleTag: 换源
sidebar: true
article: true
comment: true
description: 更换node.js国内镜像源,确保能够通过国内的网络通畅的访问
coverImg: https://raw.githubusercontent.com/matrix-johnjohn/kimo-docs/refs/heads/main/images/articles/front/node/00.forest.jpg
docAnalysis: true
inCatalogue: true
autoTitle: true
articleUpdate: true
inHomePost: true
---

## 安装依赖
将zustand安装到项目中;
````bash
npm install zustand

pnpm install zustand
````

## 变量存储
你的存储是一个钩子,你可以将任何东西放入其中;
````js
import { create } from 'zustand';

const useStore = create(((set) => ({
    count: 0,
    increase: () => set((state: { count: number; }) => ({ count: state.count + 1 })),
    removeAll: () => set({ count: 0 }),
    updateBears: (newCount: number) => set({ count: newCount }),
})));

export default useStore;
````

## 缓存存储
persist 中间件允许你在页面重新加载或应用重新启动时保留存储的状态;
````js
import { create } from 'zustand'
import { persist } from 'zustand/middleware'

const useStore = create(persist((set) => ({
    count: 0,
    increase: () => set((state: { count: number; }) => ({ count: state.count + 1 })),
    removeAll: () => set({ count: 0 }),
    updateBears: (newCount: number) => set({ count: newCount }),
}),{ name: 'count' }));

export default useStore;
````
