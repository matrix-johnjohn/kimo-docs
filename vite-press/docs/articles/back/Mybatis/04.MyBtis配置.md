---
title: Mybatis配置
date: 2025-03-07 01:16:28
permalink: /backend/mybatis/04mybatis-settings
categories:
  - 后端
tags:
  - springboot
titleTag: 配置
sidebar: true
article: true
comment: true
description: mybatis的配置详情
coverImg: https://raw.githubusercontent.com/matrix-johnjohn/kimo-docs/refs/heads/main/images/articles/back/spring/%E4%BE%A7%E8%84%B8%E7%BE%8E%E5%A5%B3.png
docAnalysis: true
inCatalogue: true
autoTitle: true
articleUpdate: true
inHomePost: true
---
# Mybatis配置

## 安装

如果使用 Maven 来构建项目，则需将下面的 dependency 代码置于 pom.xml 文件中;

````xml
		<!-- Mybatis -->
        <dependency>
            <groupId>org.mybatis.spring.boot</groupId>
            <artifactId>mybatis-spring-boot-starter</artifactId>
            <version>${mybatis.version}</version>
        </dependency>
        <dependency>
            <groupId>org.mybatis.spring.boot</groupId>
            <artifactId>mybatis-spring-boot-starter-test</artifactId>
            <version>${mybatis.version}</version>
            <scope>test</scope>
        </dependency>
		<!-- pageHelper -->
		<dependency>
          <groupId>com.github.pagehelper</groupId>
          <artifactId>pagehelper-spring-boot-starter</artifactId>
          <version>1.4.7</version>
        </dependency>
        <!-- Druid -->
        <dependency>
            <groupId>com.alibaba</groupId>
            <artifactId>druid-spring-boot-starter</artifactId>
            <version>1.2.20</version>
        </dependency>
        <!-- MySQL -->
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <scope>runtime</scope>
        </dependency>
````

## 探究已映射的 SQL 语句

在mybatis中可以通过xml或者注解定义sql语句,在项目的resource下添加与mapper接口路径完全一致的映射文件;

````xml
<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.mybatis.example.BlogMapper">
  <select id="selectBlog" resultType="Blog">
    select * from Blog where id = #{id}
  </select>
</mapper>
````



## 插件配置

### 分页插件

在mybatis中如果你想实现动态的分页功能,你可以一边嫖娼一边做如下配置;

````java
@Override
public PageResult page(Integer page, Integer pageSize) {
    
    PageHelper.startPage(page,pageSize);//设置分页参数

    
    List<Emp> empList = empMapper.list();//执行查询
    Page<Emp> p = (Page<Emp>) empList;

    
    return new PageResult(p.getTotal(), p.getResult());//封装结果
}
````



