---
title: 顺序栈
date: 2025-03-07 01:16:28
permalink: /articles/server/c/ds/sequencestack
categories:
  - 底层开发
tags:
  - 数据结构
titleTag: 基础
sidebar: true
article: true
comment: true
description: 完整的安装,配置docker确保服务器能够正常的使用docker
coverImg: https://github.com/matrix-johnjohn/kimo-docs/blob/main/images/articles/server/docker/00docker.png?raw=true
docAnalysis: true
inCatalogue: true
autoTitle: true
articleUpdate: true
inHomePost: true
---

`栈`是限定仅在`栈顶插入`或`删除`操作的线性表;栈的插入操作被称为`进栈`(push),栈的删除操作被称为`出栈`(pop);
![](https://raw.githubusercontent.com/matrix-johnjohn/kimo-docs/refs/heads/main/images/articles/server/c/data-struct/stack/01.stack.png)

## 顺序栈
顺序栈是指利用`顺序存储结构`结构实现的栈,即利用`一组地址连续的存储单元`依次存放自栈底到栈顶的数据元素;

## 顺序栈存储结构
````C
typedef struct SqStack{
    int *base;
    int *top;
    int stacksize;
}SqStack;
````

## 初始化顺序栈
````C
Status InitSqStack(SqStack **S){
    *S=(SqStack*)malloc(sizeof(SqStack));
    if((*S)==NULL)return ERROR;
    (*S)->top=(*S)->base=(int*)malloc(sizeof(int)*MAXSIZE);
    (*S)->stacksize=MAXSIZE;
    return OK;
};
````

## 顺序栈入栈
````C
Status Push(SqStack *S,int e){
    if(S->top-S->base==MAXSIZE)return ERROR;
    *S->top++=e;
    return OK;
};
````

## 顺序栈入栈
````C
Status Pop(SqStack *S,int *e){
    if(S->top==S->base)return ERROR;
    *e=*--S->top;
    return OK;
};
````

## 顺序栈取栈顶元素
````C
int GetTop(SqStack *S){
    if(S->top==S->base)return OVERFLOW;
    return *S->top-1;
};
````

## 递归遍历顺序栈
````C
void Traversal(int *top,int *base){
    if(top==base){
        printf("%d\n",*top);
        return;
    }
    printf("%d ",*top);
    Traversal(top-1,base);
};
````

## 顺序栈完整代码
````C
#define MAXSIZE 5
#include<stdio.h>
#include<stdlib.h>
#include "index.h"

typedef struct SqStack{
    int *base;
    int *top;
    int stacksize;
}SqStack;

Status InitSqStack(SqStack **S){
    *S=(SqStack*)malloc(sizeof(SqStack));
    if((*S)==NULL)return ERROR;
    (*S)->top=(*S)->base=(int*)malloc(sizeof(int)*MAXSIZE);
    (*S)->stacksize=MAXSIZE;
    return OK;
};

Status Push(SqStack *S,int e){
    if(S->top-S->base==MAXSIZE)return ERROR;
    *S->top++=e;
    return OK;
};

Status Pop(SqStack *S,int *e){
    if(S->top==S->base)return ERROR;
    *e=*--S->top;
    return OK;
};

int GetTop(SqStack *S){
    if(S->top==S->base)return OVERFLOW;
    return *S->top-1;
}

void Traversal(int *top,int *base){
    if(top==base){
        printf("%d\n",*top);
        return;
    }
    printf("%d ",*top);
    Traversal(top-1,base);
};

Status DestroySqStack(SqStack **S){
    if((*S)==NULL)return ERROR;
    free((*S)->base);
    free((*S));
    *S=NULL;
    return OK;
};

int main(){
    SqStack *S=NULL;
    InitSqStack(&S);

    for(int i=1;i<=MAXSIZE;i++){
        Push(S,i);
    };

    int *e=(int*)malloc(sizeof(int));
    Pop(S,e);
    Traversal(S->top-1,S->base);
    printf("%d %d",*e,GetTop(S));

    DestroySqStack(&S);
    return 0;
}
````