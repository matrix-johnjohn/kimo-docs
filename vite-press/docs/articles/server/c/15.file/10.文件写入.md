---
title: 文件写入
date: 2026-01-14 21:18:28
permalink: /articles/server/c/file/write
categories:
  - 底层开发
tags:
  - c
titleTag: 基础
sidebar: true
article: true
comment: true
description: 文件的写入
coverImg: https://raw.githubusercontent.com/matrix-johnjohn/kimo-docs/refs/heads/main/images/cover/c/14.png
docAnalysis: true
inCatalogue: true
autoTitle: true
articleUpdate: true
inHomePost: true
---

## 字符写入
将单个字符`c`写入`文本文件`中,成功后将写入的字符返回为int,失败则返回`EOF`;
````c
int fputc(int c, FILE *stream);
````

## 字符串写入
将字符串`s`写入`文本文件`中,成功后返回非负数,失败则返回`EOF`;
````c
int fputs(const char *s, FILE *stream);
````

## 字符串格式化写入
按格式字符串format以format指定的方式将数据写入`文本文件`;
````c
int fprintf(FILE *stream, const char *format, ...);
````

## 二进制写入
将nmemb个元素写入二进制文件中
````c
size_t fwrite(const void *ptr, size_t size, size_t nmemb, FILE *stream);
````