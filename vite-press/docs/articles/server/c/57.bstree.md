---
title: 二叉排序树
date: 2026-01-14 02:02:30
permalink: /articles/server/c/ds/search/bts
categories:
  - 底层开发
tags:
  - 数据结构
titleTag: 基础
sidebar: true
article: true
comment: true
description: 二分查找的细节查找
coverImg: https://github.com/matrix-johnjohn/kimo-docs/blob/main/images/articles/server/docker/00docker.png?raw=true
docAnalysis: true
inCatalogue: true
autoTitle: true
articleUpdate: true
inHomePost: true
---

二叉排序树又称二叉查找树,是一种对排序和查找都很有用的特殊二叉树;在二叉排序树中,`当前根节点`的左子树的结点的值比当前根节点小,`当前根节点`的右子树的结点的值比当前根节点大;

## 数据元素类型
二叉排序树的二叉链表存储表示;
```` c
typedef struct BSTNode{
    int key;
    struct BSTNode* left;
    struct BSTNode* right;
}BSTNode,*BSTree;
````

## 二叉排序树查找
二叉排序树可以看成一个有序表,所以在二叉排序树上进行查找和`折半查找`类似,也是一个逐步缩小查找范围的过程;
````c
BSTNode* SearchBST(BSTNode* root,int key){
    if((root==NULL)||root->key==key)return root;
    else if(root->key<key)return SearchBST(root->right,key);
    else if(root->key>key)return SearchBST(root->left,key);
};
````

## 二叉排序树的插入
二叉排序树的插入操作是基于查找,要将一个关键字为key的结点插入二叉排序树中,则需要从根结点向下查找,当树中不存在的时候添加一个叶子结点;
```` c
void InsertBST(BSTree* BT,int key){
    if(*BT==NULL){
        BSTNode* S=(BSTNode*)malloc(sizeof(BSTNode));
        S->key=key;
        S->left=S->right=NULL;
        *BT=S;
    }else if(key<(*BT)->key){
        InsertBST((&(*BT)->left),key);
    }else if(key>(*BT)->key){
        InsertBST(&((*BT)->right),key);
    }
};
````

## 二叉排序树删除
被删除的结点可能是二叉排序树中
```` c

````