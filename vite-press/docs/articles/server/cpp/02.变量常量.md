---
title: 变量和常量
date: 2025-03-07 01:16:28
permalink: /articles/server/cpp/params
categories:
  - 服务端
tags:
  - cpp
titleTag: 底层
sidebar: true
article: true
comment: true
description: 完整的安装,配置docker确保服务器能够正常的使用docker
coverImg: https://github.com/matrix-johnjohn/kimo-docs/blob/main/images/articles/server/docker/00docker.png?raw=true
docAnalysis: true
inCatalogue: true
autoTitle: true
articleUpdate: true
inHomePost: true
---

## 宏
宏是C预处理器提供的一种文本替换机制,它在编译之前对源代码进行扫描和替换,因此宏本身不会占用程序运行时的内存;<br>
`_CRT_SECURE_NO_WARNINGS` 是 Visual Studio 中的一个宏，用于禁用对 strcpy、strcat 等“不安全”C 标准库函数的编译警告。它不影响代码功能，仅关闭安全提示，必须在包含头文件前定义。该宏仅在 MSVC 有效，不影响 GCC/Clang，且不提升代码安全性，需开发者自行确保缓冲区安全。
```` C
#include<stdio.h>
#define PI 3.141592653589//宏

int main() {
	printf("%.2f",PI);
	return 0;
} 
````

## 变量和常量
变量本质是一块内存区域的别名,用来存储程序运行时所需要的数据;当你定义一个变量时,编译器会根据数据类型分配相应的内存空间;

```` C
#include<stdio.h>

int main() {
	int age = 20;//变量赋值
	const double PI = 3.141592653589;//常量赋值
	printf("%d %.2f", age,PI);
	return 0;
} 
````


## 内存分配与初始化
C通常情况下不会自动把分配到的内存。如果定义变量时没有显式赋值，变量里存的不是`0`，而是内存原本残留的随机数据，也就是所谓的`垃圾值`（Garbage Value）
